namespace Mir.DiscordExtension
{
    public enum StatusType
    {
        PlayerCount,
        ObserverCount,
        GameState,
        Party,
        SmallImage,
        SmallImageText,
        LargeImage,
        LargeImageText,
        PlayerName,
        PlayerLevel,
        PlayerClass
    }
}